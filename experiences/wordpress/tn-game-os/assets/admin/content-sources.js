(function($){'use strict';$(function(){$('[data-tng-source-sync]').on('click',async function(){const button=$(this),result=$('[data-tng-source-result]'),provider=button.data('tng-source-sync'),postId=button.data('post-id'),externalId=$('#tng-source-google-id').val().trim();if(!externalId){result.text('Enter a source ID first.').addClass('is-error');return;}button.prop('disabled',true);result.removeClass('is-error is-success').text('Synchronizing…');try{const response=await fetch(TNG_CONTENT_SOURCES.ajaxUrl,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded; charset=UTF-8'},body:new URLSearchParams({action:'tng_os_sync_content_source',nonce:TNG_CONTENT_SOURCES.nonce,provider:provider,post_id:postId,external_id:externalId}).toString()});const payload=await response.json();result.text(payload.data&&payload.data.message?payload.data.message:'Sync completed.').toggleClass('is-success',!!payload.success).toggleClass('is-error',!payload.success);}catch(e){result.text('Content source sync failed.').addClass('is-error');}finally{button.prop('disabled',false);}});});})(jQuery);